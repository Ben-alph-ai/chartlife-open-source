/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    // Expose generic env vars if needed, but keys should stay server-side mostly
  }
};

export default nextConfig;